{
  "name": "@sovereon/shared",
  "version": "1.0.0",
  "description": "Shared types and validation schemas for Sovereon",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.20.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
